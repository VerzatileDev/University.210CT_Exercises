#include "Task2.h"

	
	/*
	
	// Big O  of this nested loop is  O(n^2) meaning O(n) linear time
	Function ADDITION (parameter1, parameter2)

		
		if matrix1 && matrix2 != rows x columns;// We check if matrix1, matrix2 has same amount of row and columns
			return -1; // Cannot substract matricies
		else // Start Processing , addition matrix1 with matrix2 to result.

		for i <- 1 to < rows // Get row size reached
			for j <- 2; j < columns // Run until Column size reached
				result[i][j] <- matrix1[i][j] + matrix2[i][j]; // <-  " means = "
			return result;
	End function // Exit

	// Big O  of this nested loop is  O(n^2) meaning O(n) linear time
	Function SUBSTRACTION (parameter1, parameter2)

		if matrix1 && matrix2 != rows x columns//Check Matrix sizes 
			return -1; // Cannot substract matricies
		else

		for i <- 1 to < rows // Run until row size reached
			for  j <- 2 to < columns // Run until Column size reached
				result[i][j] <- matrix1[i][j] - matrix2[i][j] // <-  " means = "
		return result
 
	END function // Exit

	// triple Nested loop Bio is  O(n^3) meaning o(n) linear time. as it has 3 Loops.
	Function MULTIPLICATION (parameter1, parameter2)
		
		for i <- 1 to < rows 
			for j <- 1 to columns
				for n <- 1 to columns 
					result <- result + matrix1[i][n] * matrix2[n][j]
				Dmatrix[i][j] <- result
		return Dmatrix
	END function // Exit
	
	function QUATRATICFORM(parameter1, n)
		// Multiply n times to itself.
		matrix[][] <- parameter1
		
		while i <-1 to < n
			for i <- 1 to < rows 
			for j <- 1 to columns
				for n <- 1 to columns 
					result <- result + matrix[i][n] * matrix[n][j]
				Dmatrix[i][j] <- result
		return Dmatrix


	function COMPUTEFORMULA()
		
	*/